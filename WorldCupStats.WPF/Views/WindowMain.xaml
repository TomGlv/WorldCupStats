<Window x:Class="WorldCupStats.WPF.Views.WindowMain"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="World Cup Statistics" Height="720" Width="1280"
        WindowStartupLocation="CenterScreen"
        Closing="Window_Closing">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Menu -->
        <Menu Grid.Row="0">
            <MenuItem Header="File">
                <MenuItem Header="Settings" Click="MenuSettings_Click"/>
            </MenuItem>
        </Menu>

        <!-- Sélection équipes -->
        <Grid Grid.Row="1" Margin="15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Équipe 1 -->
            <StackPanel Grid.Column="0">
                <TextBlock Text="Home Team" FontWeight="Bold" 
                           Margin="0,0,0,5"/>
                <ComboBox x:Name="cmbHomeTeam" Height="32"
                          SelectionChanged="CmbTeam_SelectionChanged"/>
                <Button Content="Team Info" Margin="0,5,0,0"
                        Height="30" Click="BtnHomeTeamInfo_Click"/>
            </StackPanel>

            <!-- Score -->
            <StackPanel Grid.Column="1" 
                        VerticalAlignment="Center" 
                        Margin="20,0">
                <TextBlock x:Name="lblScore" 
                           Text="- : -" 
                           FontSize="32" FontWeight="Bold"
                           HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- Équipe 2 -->
            <StackPanel Grid.Column="2">
                <TextBlock Text="Away Team" FontWeight="Bold" 
                           Margin="0,0,0,5"/>
                <ComboBox x:Name="cmbAwayTeam" Height="32"
                          SelectionChanged="CmbTeam_SelectionChanged"/>
                <Button Content="Team Info" Margin="0,5,0,0"
                        Height="30" Click="BtnAwayTeamInfo_Click"/>
            </StackPanel>
        </Grid>

        <!-- Terrain de foot -->
        <Grid Grid.Row="2" Margin="15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Moitié gauche - Home -->
            <Border Grid.Column="0" Background="#2d5a1b" 
                    Margin="0,0,2,0" CornerRadius="4">
                <StackPanel x:Name="panelHomeTeam" 
                            Margin="10"/>
            </Border>

            <!-- Moitié droite - Away -->
            <Border Grid.Column="1" Background="#2d5a1b" 
                    Margin="2,0,0,0" CornerRadius="4">
                <StackPanel x:Name="panelAwayTeam" 
                            Margin="10"/>
            </Border>
        </Grid>

        <!-- Loading -->
        <TextBlock x:Name="lblLoading" Grid.Row="3"
                   Text="Loading..." Margin="15,5"
                   Foreground="Blue" Visibility="Collapsed"/>
    </Grid>
</Window>